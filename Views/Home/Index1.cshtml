<!DOCTYPE html>
<html lang="tr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>@ViewData["Title"] - Hukukk</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            margin: 0;
            padding: 0;
            background-color: #f9f9f9; /* Arka plan rengi */
            color: #333; /* Yazı rengi */
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
        }

        .container {
            width: 400px; /* Kutu genişliği */
            padding: 20px;
            border-radius: 10px;
            background-color: #fff; /* Kutu arka plan rengi */
            box-shadow: 0 0 20px rgba(0, 0, 0, 0.1); /* Hafif gölge efekti */
            display: flex;
            flex-direction: column;
            align-items: center;
        }

        h1 {
            text-align: center;
            margin-bottom: 20px;
            color: #007bff; /* Başlık rengi */
        }

        .form-group {
            width: 100%;
            margin-bottom: 20px;
        }

        label {
            font-weight: bold;
            margin-bottom: 5px;
            display: block;
            color: #555; /* Etiket rengi */
        }

        input[type="text"],
        input[type="email"],
        select,
        textarea {
            width: calc(100% - 22px); /* Inputların genişliğini ayarla */
            padding: 10px;
            border: 1px solid #ddd; /* Kenarlık rengi */
            border-radius: 5px;
            background-color: #f5f5f5; /* Input arka plan rengi */
            color: #333; /* Input yazı rengi */
            margin-top: 5px;
        }

        select {
            appearance: none;
            -webkit-appearance: none;
            -moz-appearance: none;
        }

        input[type="submit"] {
            width: 100%;
            background-color: #007bff;
            color: #fff;
            border: none;
            padding: 10px 20px;
            border-radius: 5px;
            cursor: pointer;
            transition: background-color 0.3s ease;
        }

            input[type="submit"]:hover {
                background-color: #0056b3;
            }

        .success-message {
            color: #28a745; /* Başarı mesajı rengi */
            font-weight: bold;
            margin-top: 20px;
            text-align: center;
            display: none;
        }
    </style>
</head>
<body>

    <div class="container">
        <h1>Destek Hattına Hoşgeldiniz</h1>
        <form method="post" id="supportForm">
            <div class="form-group">
                <label for="AdSoyad">Adı Soyadı</label>
                <input type="text" id="AdSoyad" name="AdSoyad" required>
            </div>
            <div class="form-group">
                <label for="Eposta">E-Posta</label>
                <input type="email" id="Eposta" name="Eposta" required>
            </div>
            <div class="form-group">
                <label for="SikayetTur">Şikayet Türü</label>
                <select id="SikayetTur" name="SikayetTur" required>
                    <option value="" disabled selected>Seçiniz</option>
                    <option value="Öneri">Öneri</option>
                    <option value="Hata">Hata</option>
                    <option value="Diğer">Diğer</option>
                </select>
            </div>
            <div class="form-group">
                <label for="Aciklama">Açıklama</label>
                <textarea id="Aciklama" name="Aciklama" rows="5" required></textarea>
            </div>
            <div class="form-group">
                <input type="submit" value="Gönder" id="submitBtn">
            </div>
        </form>
        <div class="success-message" id="successMessage">
            Gönderim başarılı. Geri bildiriminiz için teşekkürler.
        </div>
    </div>

    <script>
        document.getElementById("supportForm").addEventListener("submit", function (event) {
            event.preventDefault(); // Formun varsayılan davranışını engelle

            // Başarı mesajını göster
            var successMessage = document.getElementById("successMessage");
            successMessage.style.display = "block";

            // 4 saniye sonra mesajı kapat
            setTimeout(function () {
                successMessage.style.display = "none";
            }, 4000);
        });
    </script>
</body>
</html>